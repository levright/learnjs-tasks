/*
Этот цикл – бесконечный. Он никогда не завершится, почему?
*/

let i = 0;
while (i != 10) {
  i += 0.2;
}

// Потому что дробные числа при хранении теряют точность на больших разрядах,
// i никогда не будет равным 10