/*
Здесь функция makeUser возвращает объект.

Каким будет результат при обращении к свойству объекта ref? Почему?
*/

function makeUser() {
  return {
    name: "John",
    ref: this
  };
}

let user = makeUser();

alert( user.ref.name ); /* ошибка, потому что в момент вызова функции объект
this не существует, поэтому поле ref будет иметь значение undefined,
а попытки обращений к любым полям объекта undefined вернут ошибку*/